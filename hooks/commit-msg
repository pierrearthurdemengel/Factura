#!/bin/sh
# Bloque le commit si une ligne Co-authored-by est presente.
if grep -iE '^Co-authored-by:' "$1" > /dev/null 2>&1; then
    echo "Erreur : Co-authored-by detecte. Supprimer la ligne et recommencer."
    exit 1
fi
